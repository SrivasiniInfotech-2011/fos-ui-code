import { TestBed } from '@angular/core/testing';

import { FOSApiValidatorServiceService } from './fosapi-validator-service.service';

describe('FOSApiValidatorServiceService', () => {
  let service: FOSApiValidatorServiceService;

  // beforeEach(() => {
  //   TestBed.configureTestingModule({});
  //   service = TestBed.inject(FOSApiValidatorServiceService);
  // });

  it('should be created', () => {
    // expect(service).toBeTruthy();
  });
});
